# Custom Agent Rules

- Rebuild Docker containers (`docker compose up --build`) after every code change or feature modification to ensure the running Docker stack reflects the latest codebase updates.

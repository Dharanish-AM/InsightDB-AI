[pytest]
asyncio_mode = auto
markers =
    api: API-level end-to-end tests (require running backend at localhost:8000)
    browser: Browser-level end-to-end tests (require running frontend at localhost:5173)
    slow: Tests that are slow or require a live LLM
